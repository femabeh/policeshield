Config = {}

Config.Locale = 'en' --de or en

Config.Command = "shield" --Command that you enter in the chat

Config.Item = "shield" --Name of the Item that you need to get the Shield

Config.Jobs = { --Jobs that can use the Shield
    "police",
}
